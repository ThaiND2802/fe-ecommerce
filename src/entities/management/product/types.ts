export interface ProductItem {
  id: string
  code: string
  name: string
  description: string
  unit: string
  price: number
  cost_price: number
  image_url: string
  category_id: string
  is_active: boolean
}
